import { createRoot } from "react-dom/client.js";
import React from "react.js";
import { ReactLandingPage } from "./Views/ReactLandingPage.js";

const root = createRoot(window.bodyTag);
 root.render(
 <main>
   <ReactLandingPage />
 </main>
 );
   
